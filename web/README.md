This folder is reserved for optional ComfyUI frontend extensions (served via `WEB_DIRECTORY` in `__init__.py`).

Keep it in the repo even if empty so ComfyUI doesn't error when registering the web directory.
